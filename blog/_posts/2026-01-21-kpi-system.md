---
layout: default
title: "서울경제신문 기자 정량평가 시스템 개발기"
date: 2026-01-21
---

# 서울경제신문 기자 정량평가 시스템 개발기

> 인턴 첫 프로젝트로 기획부터 배포까지 단독으로 진행한 풀스택 웹 애플리케이션 개발 경험을 공유합니다.

---

## 📌 프로젝트 배경

신문사에서는 기자들의 성과를 정량적으로 평가하기 위해 기사 수, 글자 수, 지면 배치 등의 데이터를 집계합니다. 기존에는 이 과정이 수작업으로 진행되어 많은 시간이 소요되었고, 휴먼 에러의 가능성도 있었습니다.

이 문제를 해결하기 위해 **XML 데이터를 자동으로 파싱하고, 기자별 통계를 실시간으로 조회할 수 있는 웹 시스템**을 개발하게 되었습니다.

---

## 🏗️ 시스템 아키텍처

```
┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│  XML Data   │────▶│   Lambda    │────▶│  data.json  │
│    (S3)     │     │  (Python)   │     │    (S3)     │
└─────────────┘     └─────────────┘     └─────────────┘
                           ▲                    │
                           │                    ▼
                    ┌──────┴──────┐     ┌─────────────┐
                    │ EventBridge │     │  Frontend   │
                    │ (Schedule)  │     │   (HTML)    │
                    └─────────────┘     └─────────────┘
```

### 기술 스택

| 영역 | 기술 |
|------|------|
| Frontend | HTML, CSS, JavaScript, Bootstrap 5, Chart.js |
| Backend | AWS Lambda (Python 3.9) |
| Storage | AWS S3 (정적 웹 호스팅 + 데이터 저장) |
| Scheduling | AWS EventBridge |
| Library | SheetJS (엑셀 내보내기) |

---

## 🔧 주요 기능 구현

### 1. XML 데이터 자동 파싱

매일 오전 6시, 오후 6시에 EventBridge가 Lambda 함수를 트리거합니다. Lambda는 S3에 저장된 XML 파일을 파싱하여 기자별 기사 데이터를 JSON으로 변환합니다.

```python
def extract_reporters(author_str):
    """공동 취재 기사에서 기자명 분리"""
    # 지역=이름 형식 처리 (베이징=송종호 → 송종호)
    author_str = re.sub(r'[가-힣a-zA-Z]+\s*=\s*', '', author_str)
    # 구분자로 분리 (·, ,, /)
    names = re.split(r'[·,/]', author_str)
    return [name.strip() for name in names if name.strip()]
```

### 2. 중복 기사 제거

같은 기사가 제목만 수정되어 중복 등록되는 경우를 방지하기 위해 URL 기준으로 중복을 체크합니다.

```python
# URL에서 기사 ID 추출하여 중복 체크
if '/NewsView/' in url:
    article_id = url.split('/NewsView/')[-1].split('?')[0]
```

### 3. 기간별 필터링

일별, 주별, 월별, 분기별, 반기별로 데이터를 필터링하고, 네비게이션 버튼으로 기간을 이동할 수 있습니다.

```javascript
function getFilteredArticles() {
    switch (currentPeriod) {
        case 'daily':
            return articles.filter(a => a.pub_date === formatDate(selectedDate));
        case 'weekly':
            const weekStart = getWeekStart(selectedDate);
            const weekEnd = new Date(weekStart);
            weekEnd.setDate(weekStart.getDate() + 6);
            return articles.filter(a => {
                const d = new Date(a.pub_date);
                return d >= weekStart && d <= weekEnd;
            });
        // ...
    }
}
```

### 4. 엑셀 내보내기

SheetJS 라이브러리를 활용하여 서버 없이 클라이언트에서 바로 엑셀 파일을 생성합니다.

---

## 📊 결과물

### 대시보드 화면
- 전체 통계 요약 (기사수, 기자수, 평균 글자수)
- 기사수 TOP 10 기자 순위
- 일별 기사수 증감 추이
- 일별 기사수 차트

### 기자 상세 화면
- 기간별 기사 목록
- 면별 분포 시각화
- 정성 평가 입력 (취재유형, 임팩트 등급)
- 엑셀 내보내기

---

## 💡 배운 점

### 기술적 성장
- **AWS 서버리스 아키텍처**: Lambda, S3, EventBridge를 조합한 비용 효율적인 시스템 설계
- **XML 파싱**: 다양한 형식의 XML 데이터를 유연하게 처리하는 방법
- **프론트엔드 개발**: Bootstrap과 Chart.js를 활용한 반응형 대시보드 구현

### 프로젝트 관리
- 요구사항 분석부터 배포까지 전체 개발 사이클 경험
- 실제 사용자 피드백을 반영한 기능 개선
- 문서화의 중요성 (노션으로 진행 상황 공유)

---

## 🔗 Links

- **GitHub**: [journalist-evaluation](https://github.com/gkfla2020-bit/journalist-evaluation)
- **Live Demo**: [kpi.sedaily.ai](https://kpi.sedaily.ai)
